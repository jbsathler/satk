/* Copyright (C) 2010, University of Halle */

/*  This file is part of Sather-K Compiler Halle.
      
    Sather-K Compiler Halle is free software: you can redistribute it and/or modify
    it under the terms of the GNU General Public License as published by
    the Free Software Foundation, either version 3 of the License, or
    (at your option) any later version.
                      
    Sather-K Compiler Halle is distributed in the hope that it will be useful,
    but WITHOUT ANY WARRANTY; without even the implied warranty of
    MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
    GNU General Public License for more details.
                                      
    You should have received a copy of the GNU General Public
    License along with Sather-K Compiler Halle.  If not, see
    <http://www.gnu.org/licenses/>. */
    


#define _TERMACT_LST_Class_declarationCLASS_DECLARATIONS

#define _TERMACT_LST_0CLASS_DECLARATIONS

#define _TERMACT_LST_Parameter_typePARAMETER_TYPES

#define _TERMACT_LST_0PARAMETER_TYPES

#define _TERMACT_LST_BoundBOUNDS

#define _TERMACT_LST_0BOUNDS

#define _TERMACT_LST_Named_expressionNAMED_EXPRESSIONS

#define _TERMACT_LST_0NAMED_EXPRESSIONS

#define _TERMACT_LST_ModificationMODIFICATIONS

#define _TERMACT_LST_0MODIFICATIONS

#define _TERMACT_LST_Feature_declarationFEATURE_DECLS

#define _TERMACT_LST_0FEATURE_DECLS

#define _TERMACT_LST_Parameter_specificationPARAMETERS

#define _TERMACT_LST_0PARAMETERS

#define _TERMACT_LST_Label_rangeLABEL_LIST

#define _TERMACT_LST_0LABEL_LIST

#define _TERMACT_LST_Class_declaration_2CLASS_DECLARATIONS_2

#define _TERMACT_LST_0CLASS_DECLARATIONS_2

#define _TERMACT_LST_Parameter_type_2PARAMETER_TYPES_2

#define _TERMACT_LST_0PARAMETER_TYPES_2

#define _TERMACT_LST_Bound_2BOUNDS_2

#define _TERMACT_LST_0BOUNDS_2

#define _TERMACT_LST_Named_expression_2NAMED_EXPRESSIONS_2

#define _TERMACT_LST_0NAMED_EXPRESSIONS_2

#define _TERMACT_LST_Feature_declaration_2FEATURE_DECLS_2

#define _TERMACT_LST_0FEATURE_DECLS_2

#define _TERMACT_LST_Parameter_specification_2PARAMETERS_2

#define _TERMACT_LST_0PARAMETERS_2

#define _TERMACT_LST_Label_range_2LABEL_LIST_2

#define _TERMACT_LST_0LABEL_LIST_2

#define _TERMACT_LST_class_declarationCLASS_DECLARATIONS_O

#define _TERMACT_LST_0CLASS_DECLARATIONS_O

#define _TERMACT_LST_parameter_typePARAMETER_TYPES_O

#define _TERMACT_LST_0PARAMETER_TYPES_O

#define _TERMACT_LST_boundBOUNDS_O

#define _TERMACT_LST_0BOUNDS_O

#define _TERMACT_LST_named_expressionNAMED_EXPRESSIONS_O

#define _TERMACT_LST_0NAMED_EXPRESSIONS_O

#define _TERMACT_LST_modificationMODIFICATIONS_O

#define _TERMACT_LST_0MODIFICATIONS_O

#define _TERMACT_LST_feature_declarationFEATURE_DECLS_O

#define _TERMACT_LST_0FEATURE_DECLS_O

#define _TERMACT_LST_parameter_specificationPARAMETERS_O

#define _TERMACT_LST_0PARAMETERS_O

#define _TERMACT_LST_label_rangeLABEL_LIST_O

#define _TERMACT_LST_0LABEL_LIST_O

#define _TERMACT_rule_1 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_rule_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_ASSERTION_STATEMENT_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_RAISE_STATEMENT_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_RETURN_STATEMENT_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_RESUME_STATEMENT_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_BREAK_STATEMENT_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_LOOP_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_LOOP_EXP_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_TYPES_LST_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_TYPES_EMPTY_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_WHEN_TYPES_BLOCK_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_WHEN_TYPES_BLOCKS_EMPTY_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_WHEN_TYPES_BLOCKS_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_TYPE_CASE_STATEMENT_ATT_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_TYPE_CASE_STATEMENT_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_CASE_LABEL_DES_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_CASE_LABEL_VALUE_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_LABEL_RANGE_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_LABEL_RANGE_SINGLE_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_LABEL_LIST_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_WHEN_LABEL_STMT_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_WHEN_LABEL_STMTS_EMPTY_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_WHEN_LABEL_STMTS_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_CASE_STATEMENT_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_COND_STATEMENT_ELSE_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_COND_STATEMENT_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_EXCEPTION_TYPES_LST_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_EXCEPTION_TYPES_EMPTY_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_WHEN_EXCEPTION_STMT_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_WHEN_EXCEPTION_STMTS_EMPTY_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_WHEN_EXCEPTION_STMTS_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_EXCEPTIONS_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_EXCEPTIONS_EMPTY_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_BEGIN_BLOCK_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_ASSIGNMENT_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_STATEMENTS_ASSERT_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_STATEMENTS_RAISE_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_STATEMENTS_RETURN_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_STATEMENTS_RESUME_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_STATEMENTS_EXP_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_STATEMENTS_BREAK_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_STATEMENTS_LOOP_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_STATEMENTS_TYPE_CASE_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_STATEMENTS_CASE_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_STATEMENTS_COND_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_STATEMENTS_BEGIN_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_STATEMENTS_ASSIGN_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_STATEMENTS_EMPTY_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_LOCAL_STREAM_DECL_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_LOCAL_DECLS_STREAM_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_LOCAL_DECLS_ATT_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_LOCAL_DECLS_EMPTY_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_BLOCK_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_BUILTIN_INST_CLRTYPE_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_BUILTIN_INST_TYPE_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_BUILTIN_INST_STR_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord); \
	_currn->_ATTERM_1=_TERM1;

#define _TERMACT_BUILTIN_INSTS_EMPTY_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_BUILTIN_INSTS_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_METHOD_BODY_STATIC_BUILTIN_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_METHOD_BODY_BUILTIN_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_LINKER_SYMBOL_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord); \
	_currn->_ATTERM_1=_TERM1;

#define _TERMACT_LINKER_SYMBOL_EMPTY_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_FOREIGN_LANGUAGE_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord); \
	_currn->_ATTERM_1=_TERM1;

#define _TERMACT_METHOD_BODY_FOREIGN_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_METHOD_BODY_AB_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_METHOD_BODY_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_PARAMETER_SPEC_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_PARAMETERS_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_METHOD_IDENT_DEF_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord); \
	_currn->_ATTERM_1=_TERM1; \
	_currn->_ATSym=_TERM1;

#define _TERMACT_METHOD_DECL_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_IDENTIFIERS_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_IDENTIFIERS_LST_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_ATTR_SPEC_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_ATTR_DECL_CONST_TYPE_CONST_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_ATTR_DECL_TYPE_CONST_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_ATTR_DECL_CONST_EXP_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_ATTR_DECL_EXP_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_ATTR_DECL_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_ATTR_ACCESS_SH_RO_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_ATTR_ACCESS_RO_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_ATTR_ACCESS_SH_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_FEATURE_STREAM_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_FEATURE_METH_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_FEATURE_ATT_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_FEATURE_ATT_ACC_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_FEATURE_DECL_PRIV_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_FEATURE_DECL_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_FEATURE_DECLS_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_FEATURE_ID_USE_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord); \
	_currn->_ATTERM_1=_TERM1;

#define _TERMACT_FEATURE_SPEC_STREAM_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_FEATURE_SPEC_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_IDENT_MOD_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord); \
	_currn->_ATTERM_1=_TERM1;

#define _TERMACT_MODIFICATION_NAME_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_MODIFICATION_OUT_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_MODIFICATION_ONLY_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_MODIFICATIONS_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_INHERITANCE_INC_ONLY_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_INHERITANCE_INCLUDE_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_INHERITANCE_LIKE_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_INHERITANCE_PRIV_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_INHERITANCES_LST_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_INHERITANCES_EMPTY_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_DIRECT_STREAM_CALL_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_COERCION_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_STR_CONSTANT_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord); \
	_currn->_ATTERM_1=_TERM1;

#define _TERMACT_STR_CONSTANTS_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_STR_CONSTANTS_LST_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_VALUE_VOID_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_VALUE_SED_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord); \
	_currn->_ATTERM_1=_TERM1;

#define _TERMACT_VALUE_OCT_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord); \
	_currn->_ATTERM_1=_TERM1;

#define _TERMACT_VALUE_STR_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_VALUE_CHAR_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord); \
	_currn->_ATTERM_1=_TERM1;

#define _TERMACT_VALUE_BOOL_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord); \
	_currn->_ATTERM_1=_TERM1;

#define _TERMACT_VALUE_FLOAT_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord); \
	_currn->_ATTERM_1=_TERM1;

#define _TERMACT_VALUE_INT_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord); \
	_currn->_ATTERM_1=_TERM1;

#define _TERMACT_BOUND_METHOD_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAMED_EXPRESSION_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAMED_EXPRESSIONS_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_AGG_ID_DEF_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord); \
	_currn->_ATTERM_1=_TERM1;

#define _TERMACT_AGGREGATE_NAMED_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_AGGREGATE_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_TYPE_CONSTRUCTOR_SAME_AGG_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_TYPE_CONSTRUCTOR_SAME_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_TYPE_CONSTRUCTOR_DES_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_TYPE_CONSTRUCTOR_AGG_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_TYPE_CONSTRUCTOR_CLASS_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_TYPE_CONSTRUCTOR_CL_AGG_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_QUAL_FEATURE_ID_USE_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord); \
	_currn->_ATTERM_1=_TERM1;

#define _TERMACT_QUALIFICATION_TYPE_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_QUALIFICATION_EXP_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_STD_METHOD_CALL_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_OBJ_ID_USE_O \
	_currn->_ATTERM_1=_TERM1; \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_NOT_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_POW_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_MOD_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_DIV_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_QUOTIENT_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_STAR_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord); \
	_currn->_ATTERM_1=_TERM1;

#define _TERMACT_NAME_MINUS_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_PLUS_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_GT_EQ_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_GT_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_LT_EQ_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_LT_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_NOT_EQ_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_EQ_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_QUESTION_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_GRAVE_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_TILDE_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_STROKE_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_BACKSLASH_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_STR_IN_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_STR_OUT_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_EMPTY_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_OBJ_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_LEFT_SQ_BRACKET_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_EXPRESSION_EMPTY_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_EXPRESSION_AND_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_EXPRESSION_OR_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_EXPRESSION_STREAM_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_EXPRESSION_METH_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_EXPRESSION_COERCION_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_EXPRESSION_VALUE_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_EXPRESSION_BOUND_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_EXPRESSION_CONS_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_EXPRESSION_STAR_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord); \
	_currn->_ATTERM_1=_TERM1;

#define _TERMACT_DESIGNATOR_QUAL_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_DESIGNATOR_METH_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_DESIGNATOR_EXP_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_DESIGNATOR_NAME_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_ARGUMENT_EMPTY_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_ARGUMENT_INOUT_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_ARGUMENT_OUT_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_ARGUMENT_EXP_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_ARGUMENTS_LST_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_ARGUMENTS_EMPTY_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_BOUND_EXP_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_BOUNDS_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_CLASS_ID_USE_O \
	_currn->_ATTERM_1=_TERM1; \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_PARAMETER_KIND_INOUT_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_PARAMETER_KIND_OUT_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_PARAMETER_KIND_ONCE_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_PARAMETER_KIND_IN_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_PARAMETER_TYPE_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_PARAMETER_TYPES_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_METHOD_SPECIFICATION_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_CLASS_ARGUMENT_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_CLASS_ARGUMENTS_LST_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_CLASS_ARGUMENTS_EMPTY_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_CLASS_SPECIFIER_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_TYPE_SPECIFIER_STREAM_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_TYPE_SPECIFIER_SAME_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_TYPE_SPECIFIER_SAME_POLY_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_TYPE_SPECIFIER_PROC_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_TYPE_SPECIFIER_EMPTY_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_TYPE_SPECIFIER_DES_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_TYPE_SPECIFIER_CL_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_TYPE_SPECIFIER_CL_POLY_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_TYPE_SPECIFIERS_EMPTY_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_TYPE_SPECIFIERS_LST_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_CLASS_PARAM_ID_DEF_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord); \
	_currn->_ATTERM_1=_TERM1; \
	_currn->_ATSym=_TERM1;

#define _TERMACT_CLASS_PARAMETER_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_IDENT_DEF_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord); \
	_currn->_ATTERM_1=_TERM1;

#define _TERMACT_BOUND_IDENTIFIER_STAR_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord); \
	_currn->_ATTERM_1=_TERM1;

#define _TERMACT_BOUND_IDENTIFIER_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_CLASS_PARAMETERS_EMPTY_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_CLASS_PARAMETERS_LST_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_CLASS_BOUNDS_EMPTY_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_CLASS_BOUNDS_LST_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_CLASS_ID_DEF_O \
	_currn->_ATTERM_1=_TERM1; \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_BUILTIN_OPT_CL_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_BUILTIN_OPT_ID_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord); \
	_currn->_ATTERM_1=_TERM1;

#define _TERMACT_BUILTIN_OPT_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_BUILTIN_OPT_EMPTY_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_CLASS_BODY_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_CLASS_HEAD_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_CLASS_ATTRIBUTE_VA_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_CLASS_ATTRIBUTE_EX_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_CLASS_ATTRIBUTE_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_CLASS_ATTRIBUTE_ABVA_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_CLASS_ATTRIBUTE_AB_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_CLASS_DECLARATION_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_CLASS_DECLARATIONS_O \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_ASSERTION_STATEMENT_2

#define _TERMACT_RAISE_STATEMENT_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_RETURN_STATEMENT_2

#define _TERMACT_RESUME_STATEMENT_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_BREAK_STATEMENT_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_LOOP_2

#define _TERMACT_LOOP_EXP_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_WHEN_TYPES_BLOCK_ELSE_2

#define _TERMACT_WHEN_TYPES_BLOCK_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_WHEN_TYPES_BLOCKS_EMPTY_2

#define _TERMACT_WHEN_TYPES_BLOCKS_2

#define _TERMACT_TYPE_CASE_STATEMENT_ATT_2

#define _TERMACT_TYPE_CASE_STATEMENT_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_CASE_LABEL_DES_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_CASE_LABEL_VALUE_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_LABEL_RANGE_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_LABEL_RANGE_SINGLE_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_LABEL_LIST_2

#define _TERMACT_WHEN_LABEL_STMT_2

#define _TERMACT_WHEN_LABEL_STMTS_EMPTY_2

#define _TERMACT_WHEN_LABEL_STMTS_2

#define _TERMACT_CASE_STATEMENT_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_COND_STATEMENT_ELSE_2

#define _TERMACT_COND_STATEMENT_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_WHEN_EXCEPTION_STMT_ELSE_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_WHEN_EXCEPTION_STMT_2

#define _TERMACT_WHEN_EXCEPTION_STMTS_EMPTY_2

#define _TERMACT_WHEN_EXCEPTION_STMTS_2

#define _TERMACT_EXCEPTIONS_2

#define _TERMACT_EXCEPTIONS_EMPTY_2

#define _TERMACT_BEGIN_BLOCK_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_ASSIGNMENT_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_STATEMENTS_ASSERT_2

#define _TERMACT_STATEMENTS_RAISE_2

#define _TERMACT_STATEMENTS_RETURN_2

#define _TERMACT_STATEMENTS_RESUME_2

#define _TERMACT_STATEMENTS_EXP_2

#define _TERMACT_STATEMENTS_BREAK_2

#define _TERMACT_STATEMENTS_LOOP_2

#define _TERMACT_STATEMENTS_TYPE_CASE_2

#define _TERMACT_STATEMENTS_CASE_2

#define _TERMACT_STATEMENTS_COND_2

#define _TERMACT_STATEMENTS_BEGIN_2

#define _TERMACT_STATEMENTS_ASSIGN_2

#define _TERMACT_STATEMENTS_EMPTY_2

#define _TERMACT_LOCAL_STREAM_DECL_2

#define _TERMACT_LOCAL_DECLS_STREAM_2

#define _TERMACT_LOCAL_DECLS_ATT_2

#define _TERMACT_LOCAL_DECLS_EMPTY_2

#define _TERMACT_BLOCK_2

#define _TERMACT_BUILTIN_INST_CLRTYPE_2

#define _TERMACT_BUILTIN_INST_TYPE_2

#define _TERMACT_BUILTIN_INST_STR_2 \
	_currn->_ATTERM_1=_TERM1;

#define _TERMACT_BUILTIN_INSTS_EMPTY_2

#define _TERMACT_BUILTIN_INSTS_2

#define _TERMACT_METHOD_BODY_STATIC_BUILTIN_2

#define _TERMACT_METHOD_BODY_BUILTIN_2

#define _TERMACT_LINKER_SYMBOL_2

#define _TERMACT_LINKER_SYMBOL_EMPTY_2

#define _TERMACT_FOREIGN_LANGUAGE_2

#define _TERMACT_METHOD_BODY_FOREIGN_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_METHOD_BODY_AB_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_METHOD_BODY_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_PARAMETER_SPEC_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_PARAMETERS_2

#define _TERMACT_METHOD_IDENT_DEF_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord); \
	_currn->_ATTERM_1=_TERM1; \
	_currn->_ATSym=_TERM1;

#define _TERMACT_METHOD_DECL_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_IDENTIFIERS_2

#define _TERMACT_IDENTIFIERS_LST_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_ATTR_SPEC_2

#define _TERMACT_ATTR_DECL_CONST_TYPE_CONST_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_ATTR_DECL_TYPE_CONST_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_ATTR_DECL_CONST_EXP_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_ATTR_DECL_EXP_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_ATTR_DECL_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_ATTR_ACCESS_SH_RO_2

#define _TERMACT_ATTR_ACCESS_RO_2

#define _TERMACT_ATTR_ACCESS_SH_2

#define _TERMACT_FEATURE_STREAM_2

#define _TERMACT_FEATURE_METH_2

#define _TERMACT_FEATURE_ATT_2

#define _TERMACT_FEATURE_ATT_ACC_2

#define _TERMACT_FEATURE_DECL_PRIV_2

#define _TERMACT_FEATURE_DECL_2

#define _TERMACT_FEATURE_DECLS_2

#define _TERMACT_INHERITANCE_LIKE_2

#define _TERMACT_INHERITANCES_LST_2

#define _TERMACT_INHERITANCES_EMPTY_2

#define _TERMACT_DIRECT_STREAM_CALL_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_COERCION_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_STR_CONSTANT_2 \
	_currn->_ATTERM_1=_TERM1;

#define _TERMACT_STR_CONSTANTS_2

#define _TERMACT_STR_CONSTANTS_LST_2

#define _TERMACT_VALUE_VOID_2

#define _TERMACT_VALUE_SED_2 \
	_currn->_ATTERM_1=_TERM1;

#define _TERMACT_VALUE_OCT_2 \
	_currn->_ATTERM_1=_TERM1;

#define _TERMACT_VALUE_STR_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_VALUE_CHAR_2 \
	_currn->_ATTERM_1=_TERM1; \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_VALUE_BOOL_2 \
	_currn->_ATTERM_1=_TERM1;

#define _TERMACT_VALUE_FLOAT_2 \
	_currn->_ATTERM_1=_TERM1;

#define _TERMACT_VALUE_INT_2 \
	_currn->_ATTERM_1=_TERM1;

#define _TERMACT_BOUND_METHOD_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAMED_EXPRESSION_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAMED_EXPRESSIONS_2

#define _TERMACT_AGG_ID_DEF_2 \
	_currn->_ATTERM_1=_TERM1; \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_AGGREGATE_NAMED_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_AGGREGATE_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_TYPE_CONSTRUCTOR_SAME_AGG_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_TYPE_CONSTRUCTOR_SAME_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_TYPE_CONSTRUCTOR_DES_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_TYPE_CONSTRUCTOR_AGG_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_TYPE_CONSTRUCTOR_CLASS_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_TYPE_CONSTRUCTOR_CL_AGG_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_QUAL_FEATURE_ID_USE_2 \
	_currn->_ATTERM_1=_TERM1; \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_QUALIFICATION_TYPE_2

#define _TERMACT_QUALIFICATION_EXP_2

#define _TERMACT_STD_METHOD_CALL_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_OBJ_ID_USE_2 \
	_currn->_ATSym=_TERM1;

#define _TERMACT_NAME_NOT_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_POW_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_MOD_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_DIV_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_QUOTIENT_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_STAR_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_MINUS_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_PLUS_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_GT_EQ_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_GT_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_LT_EQ_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_LT_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_EQ_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_QUESTION_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_GRAVE_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_TILDE_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_STROKE_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_BACKSLASH_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_STR_IN_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_STR_OUT_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_EMPTY_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_OBJ_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_LEFT_SQ_BRACKET_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_EXPRESSION_AND_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_EXPRESSION_OR_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_EXPRESSION_STREAM_2

#define _TERMACT_EXPRESSION_METH_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_EXPRESSION_COERCION_2

#define _TERMACT_EXPRESSION_VALUE_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_EXPRESSION_BOUND_2

#define _TERMACT_EXPRESSION_CONS_2

#define _TERMACT_EXPRESSION_STAR_2

#define _TERMACT_DESIGNATOR_QUAL_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_DESIGNATOR_METH_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_DESIGNATOR_EXP_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_DESIGNATOR_NAME_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_ARGUMENT_EMPTY_2

#define _TERMACT_ARGUMENT_INOUT_2

#define _TERMACT_ARGUMENT_OUT_2

#define _TERMACT_ARGUMENT_EXP_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_ARGUMENTS_LST_2

#define _TERMACT_ARGUMENTS_EMPTY_2

#define _TERMACT_BOUND_EXP_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_BOUNDS_2

#define _TERMACT_CLASS_ID_USE_2 \
	_currn->_ATSym=_TERM1;

#define _TERMACT_PARAMETER_KIND_INOUT_2

#define _TERMACT_PARAMETER_KIND_OUT_2

#define _TERMACT_PARAMETER_KIND_ONCE_2

#define _TERMACT_PARAMETER_KIND_IN_2

#define _TERMACT_PARAMETER_TYPE_2

#define _TERMACT_PARAMETER_TYPES_2

#define _TERMACT_METHOD_SPECIFICATION_2

#define _TERMACT_CLASS_ARGUMENT_2

#define _TERMACT_CLASS_ARGUMENTS_LST_2

#define _TERMACT_CLASS_ARGUMENTS_EMPTY_2

#define _TERMACT_CLASS_SPECIFIER_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_TYPE_SPECIFIER_STREAM_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_TYPE_SPECIFIER_SAME_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_TYPE_SPECIFIER_SAME_POLY_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_TYPE_SPECIFIER_PROC_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_TYPE_SPECIFIER_EMPTY_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_TYPE_SPECIFIER_CL_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_TYPE_SPECIFIER_CL_POLY_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_TYPE_SPECIFIERS_EMPTY_2

#define _TERMACT_TYPE_SPECIFIERS_LST_2

#define _TERMACT_CLASS_PARAMETER_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_IDENT_DEF_2 \
	_currn->_ATTERM_1=_TERM1; \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord); \
	_currn->_ATSym=_TERM1;

#define _TERMACT_BOUND_IDENTIFIER_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_CLASS_PARAMETERS_EMPTY_2

#define _TERMACT_CLASS_PARAMETERS_LST_2

#define _TERMACT_CLASS_BOUNDS_EMPTY_2

#define _TERMACT_CLASS_BOUNDS_LST_2

#define _TERMACT_CLASS_ID_DEF_POLY_2 \
	_currn->_ATTERM_1=_TERM1;

#define _TERMACT_CLASS_ID_DEF_2 \
	_currn->_ATTERM_1=_TERM1; \
	_currn->_ATSym=_TERM1;

#define _TERMACT_BUILTIN_OPT_CL_2

#define _TERMACT_BUILTIN_OPT_ID_2 \
	_currn->_ATTERM_1=_TERM1;

#define _TERMACT_BUILTIN_OPT_2

#define _TERMACT_BUILTIN_OPT_EMPTY_2

#define _TERMACT_CLASS_BODY_2

#define _TERMACT_CLASS_HEAD_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_CLASS_ATTRIBUTE_VA_2

#define _TERMACT_CLASS_ATTRIBUTE_EX_2

#define _TERMACT_CLASS_ATTRIBUTE_2

#define _TERMACT_CLASS_ATTRIBUTE_ABVA_2

#define _TERMACT_CLASS_ATTRIBUTE_AB_2

#define _TERMACT_CLASS_DECLARATION_2 \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_CLASS_DECLARATIONS_2

#define _TERMACT_ASSERTION_STATEMENT \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_RAISE_STATEMENT \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_RETURN_STATEMENT \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_RESUME_STATEMENT \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_BREAK_STATEMENT \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_LOOP \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_LOOP_EXP \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_TYPES_LST \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_TYPES_EMPTY

#define _TERMACT_WHEN_TYPES_BLOCK \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_WHEN_TYPES_BLOCKS_EMPTY \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_WHEN_TYPES_BLOCKS

#define _TERMACT_TYPE_CASE_STATEMENT_ATT \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_TYPE_CASE_STATEMENT \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_CASE_LABEL_DES \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_CASE_LABEL_VALUE \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_LABEL_RANGE \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_LABEL_RANGE_SINGLE \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_LABEL_LIST \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_WHEN_LABEL_STMT \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_WHEN_LABEL_STMTS_EMPTY \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_WHEN_LABEL_STMTS \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_CASE_STATEMENT \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_COND_STATEMENT_ELSE \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_COND_STATEMENT \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_EXCEPTION_TYPES_LST \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_EXCEPTION_TYPES_EMPTY

#define _TERMACT_WHEN_EXCEPTION_STMT \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_WHEN_EXCEPTION_STMTS_EMPTY \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_WHEN_EXCEPTION_STMTS

#define _TERMACT_EXCEPTIONS \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_EXCEPTIONS_EMPTY \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_BEGIN_BLOCK \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_ASSIGNMENT \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_STATEMENTS_ASSERT \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_STATEMENTS_RAISE \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_STATEMENTS_RETURN \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_STATEMENTS_RESUME \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_STATEMENTS_EXP \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_STATEMENTS_BREAK \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_STATEMENTS_LOOP \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_STATEMENTS_TYPE_CASE \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_STATEMENTS_CASE \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_STATEMENTS_COND \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_STATEMENTS_BEGIN \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_STATEMENTS_ASSIGN \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_STATEMENTS_EMPTY \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_LOCAL_STREAM_DECL \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_LOCAL_DECLS_STREAM \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_LOCAL_DECLS_ATT \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_LOCAL_DECLS_EMPTY \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_BLOCK \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_BUILTIN_INST_CLRTYPE \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_BUILTIN_INST_TYPE \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_BUILTIN_INST_STR \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord); \
	_currn->_ATTERM_1=_TERM1;

#define _TERMACT_BUILTIN_INSTS_EMPTY \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_BUILTIN_INSTS \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_METHOD_BODY_STATIC_BUILTIN \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_METHOD_BODY_BUILTIN \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_LINKER_SYMBOL \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord); \
	_currn->_ATTERM_1=_TERM1;

#define _TERMACT_LINKER_SYMBOL_EMPTY \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_FOREIGN_LANGUAGE \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord); \
	_currn->_ATTERM_1=_TERM1;

#define _TERMACT_METHOD_BODY_FOREIGN \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_METHOD_BODY_AB \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_METHOD_BODY \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_PARAMETER_SPEC \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_PARAMETERS \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_METHOD_IDENT_DEF \
	_currn->_ATTERM_1=_TERM1; \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_METHOD_DECL \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_IDENTIFIERS \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_IDENTIFIERS_LST \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_ATTR_SPEC \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_ATTR_DECL_CONST_TYPE_CONST \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_ATTR_DECL_TYPE_CONST \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_ATTR_DECL_CONST_EXP \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_ATTR_DECL_EXP \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_ATTR_DECL \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_ATTR_ACCESS_SH_RO \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_ATTR_ACCESS_RO \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_ATTR_ACCESS_SH \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_FEATURE_STREAM \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_FEATURE_METH \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_FEATURE_ATT \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_FEATURE_ATT_ACC \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_FEATURE_DECL_PRIV \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_FEATURE_DECL \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_FEATURE_DECLS \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_FEATURE_ID_USE \
	_currn->_ATTERM_1=_TERM1;

#define _TERMACT_FEATURE_SPEC_STREAM

#define _TERMACT_FEATURE_SPEC

#define _TERMACT_IDENT_MOD \
	_currn->_ATTERM_1=_TERM1;

#define _TERMACT_MODIFICATION_NAME \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_MODIFICATION_OUT \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_MODIFICATION_ONLY \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_MODIFICATIONS

#define _TERMACT_INHERITANCE_INC_ONLY \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_INHERITANCE_INCLUDE \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_INHERITANCE_LIKE \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_INHERITANCE_PRIV \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_INHERITANCES_LST \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_INHERITANCES_EMPTY \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_DIRECT_STREAM_CALL \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_COERCION \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_STR_CONSTANT \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord); \
	_currn->_ATTERM_1=_TERM1;

#define _TERMACT_STR_CONSTANTS \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_STR_CONSTANTS_LST \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_VALUE_VOID \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_VALUE_SED \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord); \
	_currn->_ATTERM_1=_TERM1;

#define _TERMACT_VALUE_OCT \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord); \
	_currn->_ATTERM_1=_TERM1;

#define _TERMACT_VALUE_STR \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_VALUE_CHAR \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord); \
	_currn->_ATTERM_1=_TERM1;

#define _TERMACT_VALUE_BOOL \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord); \
	_currn->_ATTERM_1=_TERM1;

#define _TERMACT_VALUE_FLOAT \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord); \
	_currn->_ATTERM_1=_TERM1;

#define _TERMACT_VALUE_INT \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord); \
	_currn->_ATTERM_1=_TERM1;

#define _TERMACT_BOUND_METHOD \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAMED_EXPRESSION \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAMED_EXPRESSIONS \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_AGG_ID_DEF \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord); \
	_currn->_ATTERM_1=_TERM1;

#define _TERMACT_AGGREGATE_NAMED \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_AGGREGATE \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_TYPE_CONSTRUCTOR_SAME_AGG \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_TYPE_CONSTRUCTOR_SAME \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_TYPE_CONSTRUCTOR_DES \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_TYPE_CONSTRUCTOR_AGG \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_TYPE_CONSTRUCTOR_CLASS \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_TYPE_CONSTRUCTOR_CL_AGG \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_QUAL_FEATURE_ID_USE \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord); \
	_currn->_ATTERM_1=_TERM1;

#define _TERMACT_QUALIFICATION_TYPE \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_QUALIFICATION_EXP \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_STD_METHOD_CALL \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_OBJ_ID_USE \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord); \
	_currn->_ATTERM_1=_TERM1;

#define _TERMACT_NAME_NOT \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_POW \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_MOD \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_DIV \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_QUOTIENT \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_STAR \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord); \
	_currn->_ATTERM_1=_TERM1;

#define _TERMACT_NAME_MINUS \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_PLUS \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_GT_EQ \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_GT \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_LT_EQ \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_LT \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_EQ \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_QUESTION \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_GRAVE \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_TILDE \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_STROKE \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_BACKSLASH \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_STR_IN \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_STR_OUT \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_EMPTY \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_OBJ \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_NAME_LEFT_SQ_BRACKET \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_EXPRESSION_AND \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_EXPRESSION_OR \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_EXPRESSION_STREAM \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_EXPRESSION_METH \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_EXPRESSION_COERCION \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_EXPRESSION_VALUE \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_EXPRESSION_BOUND \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_EXPRESSION_CONS \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_EXPRESSION_STAR \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord); \
	_currn->_ATTERM_1=_TERM1;

#define _TERMACT_DESIGNATOR_QUAL \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_DESIGNATOR_METH \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_DESIGNATOR_EXP \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_DESIGNATOR_NAME \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_ARGUMENT_EMPTY \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_ARGUMENT_INOUT \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_ARGUMENT_OUT \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_ARGUMENT_EXP \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_ARGUMENTS_LST \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_ARGUMENTS_EMPTY \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_BOUND_EXP \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_BOUNDS \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_CLASS_ID_USE \
	_currn->_ATTERM_1=_TERM1; \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_PARAMETER_KIND_INOUT \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_PARAMETER_KIND_OUT \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_PARAMETER_KIND_ONCE \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_PARAMETER_KIND_IN \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_PARAMETER_TYPE \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_PARAMETER_TYPES \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_METHOD_SPECIFICATION \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_CLASS_ARGUMENT \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_CLASS_ARGUMENTS_LST \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_CLASS_ARGUMENTS_EMPTY \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_CLASS_SPECIFIER \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_TYPE_SPECIFIER_STREAM \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_TYPE_SPECIFIER_SAME \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_TYPE_SPECIFIER_SAME_POLY \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_TYPE_SPECIFIER_PROC \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_TYPE_SPECIFIER_EMPTY \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_TYPE_SPECIFIER_CL \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_TYPE_SPECIFIER_CL_POLY \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_TYPE_SPECIFIERS_EMPTY \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_TYPE_SPECIFIERS_LST \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_CLASS_PARAMETER \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_IDENT_DEF \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord); \
	_currn->_ATTERM_1=_TERM1; \
	_currn->_ATSym=_TERM1;

#define _TERMACT_BOUND_IDENTIFIER_STAR \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_BOUND_IDENTIFIER \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_CLASS_PARAMETERS_EMPTY \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_CLASS_PARAMETERS_LST \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_CLASS_BOUNDS_EMPTY \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_CLASS_BOUNDS_LST \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_CLASS_ID_DEF \
	_currn->_ATTERM_1=_TERM1; \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_BUILTIN_OPT_CL \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_BUILTIN_OPT_ID \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord); \
	_currn->_ATTERM_1=_TERM1;

#define _TERMACT_BUILTIN_OPT \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_BUILTIN_OPT_EMPTY \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_CLASS_BODY \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_CLASS_HEAD \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_CLASS_ATTRIBUTE_VA \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_CLASS_ATTRIBUTE_EX \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_CLASS_ATTRIBUTE \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_CLASS_ATTRIBUTE_ABVA \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_CLASS_ATTRIBUTE_AB \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_CLASS_DECLARATION \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);

#define _TERMACT_CLASS_DECLARATIONS \
	_currn->_AT_pos=(_coordref ? *_coordref : NoCoord);
